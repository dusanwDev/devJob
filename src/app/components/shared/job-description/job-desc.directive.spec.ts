import { JobDescDirective } from './job-desc.directive';

describe('JobDescDirective', () => {
  it('should create an instance', () => {
    const directive = new JobDescDirective();
    expect(directive).toBeTruthy();
  });
});
